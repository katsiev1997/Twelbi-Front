query Brand($slug: String!) {
	brand(slug: $slug) {
		id
		name
		logoPath
		city
		postedCount
		rating
		phone
		whatsapp
		telegram
		isSubscribed
		isBrandOwner
		about
		reviews {
			id
			authorName
			comment
			rating
			createdAt
		}
		reviewsCount
		createdAt
	}
}
