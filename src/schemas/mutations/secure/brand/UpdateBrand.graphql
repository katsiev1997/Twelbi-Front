mutation UpdateBrand($data: BrandInput!) {
	updateBrand(data: $data) {
		id
		name
		about
		balance
		email
		phone
		whatsapp
		telegram
		logoPath
		city
		postedCount
		subscribers
		category {
			id
			name
		}
		createdAt
	}
}
