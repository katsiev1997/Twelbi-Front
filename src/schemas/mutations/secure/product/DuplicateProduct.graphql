mutation DuplicateProduct($id: Int!) {
	duplicateProduct(id: $id) {
		id
		name
		posterPath
		minPrice
		maxPrice
		rating
		ratesCount
		category {
			name
			slug
		}
		provider {
			name
			slug
			logoPath
		}
		visibility
	}
}
